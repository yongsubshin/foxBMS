# PARVIS Configuration
# Generated by /parvis:init command
# Documentation: Run /parvis:init to reconfigure

# Language Settings
language:
  # Claude response language for user-facing outputs
  # Options: ko (Korean), en (English), ja (Japanese), zh (Chinese)
  claude_output: ko

  # PARVIS Run output language for generated files
  # Affects: requirements extraction, normalization reports, phase outputs
  # Options: ko (Korean), en (English), ja (Japanese), zh (Chinese)
  parvis_run: en

  # PARVIS Docs output language for final documentation
  # Affects: Sphinx docs, HTML portal content, reports
  # Options: ko (Korean), en (English), ja (Japanese), zh (Chinese)
  parvis_docs: ko

# Metadata
metadata:
  version: "1.0.0"
  initialized: true
  last_updated: "2025-12-19T22:36:00+09:00"
